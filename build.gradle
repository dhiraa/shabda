/*
 * This file was generated by the Gradle 'init' task.
 *
 * This is a general purpose Gradle build.
 * Learn how to create Gradle builds at https://guides.gradle.org/creating-new-gradle-builds/
 *
 * Kotlin: https://kotlinlang.org/docs/reference/using-gradle.html
 * Python: https://github.com/linkedin/pygradle
 */

plugins {
    id 'com.gradle.build-scan' version '1.16'
    id 'java'
    id "org.jetbrains.kotlin.jvm" version "1.3.0"
//    id "com.linkedin.python-sdist" version "0.4.9"
}
version = '0.1.0'

subprojects {
    version = '0.1.0'
}

buildScan {
    termsOfServiceUrl = 'https://gradle.com/terms-of-service'
    termsOfServiceAgree = 'yes'
    publishAlways()
}

allprojects {
    repositories {
        mavenCentral()
        jcenter()
//        pyGradlePyPi()
    }
}

sourceSets {
    main.kotlin.srcDirs += 'src/main/myKotlin'
    main.java.srcDirs += 'src/main/myJava'
}

//python {
//    docsDir = project.file('docs').path // The location of this project's Sphinx documentation directory
//    testDir = project.file('src/shabda/').path // The location of this project's tests directory.
//    srcDir = project.file('src/shabda/').path // The location of this project's source directory.
//}

dependencies {
    compile group: 'org.hibernate', name: 'hibernate-core', version: '3.6.7.Final'
    implementation 'com.google.guava:guava:23.0'
    testImplementation 'junit:junit:4.12'
    compile "org.jetbrains.kotlin:kotlin-stdlib-jdk8"
//    python 'pypi:requests:2.9.1'
//    test 'pypi:mock:1.3.0'
}

//task copy(type: Copy, group: "Custom", description: "Copies sources to the dest directory") {
//    from "src"
//    into "dest"
//}
//
//task zip(type: Zip, group: "Archive", description: "Archives sources in a zip file") {
//    from "src/main/"
//    setArchiveName "shabda-src.zip"
//}